# Ca 2 — DOMAIN rỗng (quên `--env-file .env.production`).
# Kỳ vọng: container DỪNG HẲN ở guard, trước cả khi envsubst chạy.
services:
  nginx-candidate:
    environment:
      DOMAIN: ""
      NGINX_ADMISSION_FROZEN: "false"

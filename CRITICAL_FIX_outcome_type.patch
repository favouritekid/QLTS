# CRITICAL FIX: Enum Conversion Patch
# Apply this fix to: Backend_FastAPI/app/services/pipeline_service.py

## Problem:
Backend is still sending "NEUTRAL" (uppercase) instead of "neutral" (lowercase) to database.

## Root Cause:
Your backend code does NOT have the enum conversion logic yet.

## Solution:
Replace the `create_consultation_status` function in pipeline_service.py with this:

```python
async def create_consultation_status(
    db: AsyncSession, status_in: schemas.ConsultationStatusCreate
) -> models.ConsultationStatus:
    try:
        # 1. Kiểm tra ID
        existing_id = await db.get(models.ConsultationStatus, status_in.id)
        if existing_id:
            raise DuplicateResourceError(
                f"Consultation Status ID '{status_in.id}' already exists."
            )

        # 2. Kiểm tra 'name' đã tồn tại
        existing_name = await db.scalar(
            select(models.ConsultationStatus).where(
                models.ConsultationStatus.name == status_in.name
            )
        )
        if existing_name:
            raise DuplicateResourceError(
                f"Consultation Status name '{status_in.name}' already exists."
            )

        # 3. Kiểm tra Stage cha
        await _get_stage_by_id(
            db, status_in.stage_id
        )

        # ✅ CRITICAL FIX: Convert enum to lowercase
        create_data = status_in.model_dump(mode='python')

        # Force outcome_type to lowercase
        if "outcome_type" in create_data:
            val = create_data["outcome_type"]
            log.debug(
                "Converting outcome_type",
                original_value=val,
                original_type=type(val).__name__
            )

            # Handle ALL formats
            if isinstance(val, models.OutcomeTypeEnum):
                create_data["outcome_type"] = val.value.lower()
            elif isinstance(val, str):
                create_data["outcome_type"] = val.lower()
            elif hasattr(val, 'value'):
                create_data["outcome_type"] = str(val.value).lower()
            else:
                create_data["outcome_type"] = str(val).lower()

            log.debug(
                "Converted outcome_type",
                converted_value=create_data["outcome_type"]
            )

        # 4. Create model
        db_status = models.ConsultationStatus(**create_data)

        db.add(db_status)
        await db.commit()
        await db.refresh(db_status)

        # 5. Invalidate cache
        await invalidate_pipeline_cache()
        log.info(
            "Created new consultation status, cache invalidated",
            status_id=db_status.id
        )

        return db_status
    except Exception as e:
        await db.rollback()
        log.error("Failed to create consultation status", error=str(e), exc_info=True)
        raise e
```

## Quick Fix Steps:

1. Open: `Backend_FastAPI/app/services/pipeline_service.py`

2. Find function `create_consultation_status` (around line 360)

3. Replace from:
   ```python
   create_data = status_in.model_dump()
   ```

   To:
   ```python
   create_data = status_in.model_dump(mode='python')

   # Force outcome_type to lowercase
   if "outcome_type" in create_data:
       val = create_data["outcome_type"]
       if isinstance(val, str):
           create_data["outcome_type"] = val.lower()
       elif hasattr(val, 'value'):
           create_data["outcome_type"] = str(val.value).lower()
   ```

4. Restart backend

## Verify Fix:
After restart, you should see in logs:
```
[debug] Converting outcome_type original_value=... original_type=...
[debug] Converted outcome_type converted_value=neutral
```

NOT:
```
[error] invalid input value for enum outcome_type_enum: "NEUTRAL"
```

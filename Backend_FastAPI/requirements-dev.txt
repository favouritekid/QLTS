# Development dependencies (extends production requirements)
-r requirements.txt

# Testing
Faker==35.2.2
fakeredis==2.32.0
iniconfig==2.3.0
pluggy==1.6.0
pytest==8.4.2
pytest-asyncio==1.2.0
pytest-dotenv==0.5.2
pytest-mock==3.15.1
# PR-1.6 (2026-05-24) — hard timeout guard for the full-suite gate.
# The 5-tier allowlist in backend-test.yml previously masked any test
# that hung indefinitely; full-suite restore relies on this plugin to
# enforce a per-test ceiling and abort cleanly instead of timing out
# the whole GitHub Actions job (which loses the captured logs).
pytest-timeout==2.4.0
# PDF content assertions (enrollment-letter renderer tests).
pypdf==5.1.0

# Code formatting & linting
autoflake==2.3.1
black==25.9.0
flake8==7.3.0
isort==7.0.0
mccabe==0.7.0
pathspec==0.12.1
platformdirs==4.5.0
pycodestyle==2.14.0
pyflakes==3.4.0

# Documentation
ghp-import==2.1.0
mkdocs==1.6.1
mkdocs-get-deps==0.2.0
mkdocs-material==9.6.22
mkdocs-material-extensions==1.3.1
pymdown-extensions==10.16.1
